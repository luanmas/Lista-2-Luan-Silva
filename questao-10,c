#include <stdio.h>
#include <math.h>

int main(){
    for(int i = 15; i < 90; i++){
        if(i % 4 == 0){
            float result = (int)pow(i, 2);
            printf("%.2f ", result);
        }
    }
    return 0;
}